name: validate_kit
description: >-
  A zero-dependency, fluent form validation toolkit for Dart and Flutter.
  Chain readable rules for email, password, length, range, matching fields,
  and custom logic with clean, localization-ready error messages.
version: 1.0.0
repository: https://github.com/eebrahimjoy/validate_kit
issue_tracker: https://github.com/eebrahimjoy/validate_kit/issues
homepage: https://github.com/eebrahimjoy/validate_kit

topics:
  - validation
  - form
  - validator
  - flutter
  - dart

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
